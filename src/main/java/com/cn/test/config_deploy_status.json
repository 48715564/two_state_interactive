{
  "computeNodes": 15,
  "cmms": 2,
  "unassignedComputeNodes": 15,
  "unassignedCmms": 2,
  "assignedComputeNodes": 0,
  "assignedCmms": 0
}


//code
processData: function (results) {
var status = results[0];
var profile = results[1];
var newData = {};

if ("computeNodes" in status && "items" in profile) {
// Process the data into the data needed for the dashboard category
newData.noAssignedPatternsCount = status.unassignedComputeNodes ? status.unassignedComputeNodes : 0;
newData.assignedPatternsCount = status.computeNodes ? status.computeNodes - newData.noAssignedPatternsCount : 0;

var inProgress = array.filter(profile.items, function (item) {
return item.profileStatus == "ACTIVATING";
});

newData.deploysInProgressCount = inProgress ? inProgress.length : 0;

newData.successful = true;
} else {
// An error occurred, return accordingly and display the error message
newData.successful = false;
}

this.data = newData;
return newData;
},